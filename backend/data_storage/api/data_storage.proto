syntax = "proto3";

package gateway;

option go_package = "";

service Gateway {
  rpc Proxy (stream SyncData) returns (stream SyncData) {}
}

message SyncData {
  bool cron = 1;
  bool mailLoader = 2;
  bool fileLoader = 3;
}